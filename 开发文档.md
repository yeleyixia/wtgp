# 为投个屏 - 详细开发文档

> 版本: 2.0.0 | 更新日期: 2026-08-02

## 一、项目概述

### 1.1 项目简介

「为投个屏」是一款专为 HarmonyOS 设备打造的桌面端投屏工具，基于 Python + PySide6 (Qt) 开发，通过 HDC (HarmonyOS Device Connector) 与设备通信，实现屏幕镜像、触控输入、键盘映射、音频同步、网页投屏等功能。

### 1.2 技术栈

| 组件 | 技术 | 说明 |
|------|------|------|
| GUI框架 | PySide6 6.6+ | Qt for Python，提供原生级界面渲染 |
| 设备通信 | HDC 3.2.0 | HarmonyOS 官方设备连接工具 |
| 图像处理 | OpenCV 4.8+ | JPEG 解码、视频录制 |
| 数值计算 | NumPy 1.24+ | 图像帧数据矩阵操作 |
| 音频处理 | sounddevice / pyaudio | 音频采集与播放 |
| 实时通信 | websockets 12+ | WebSocket 服务端，网页投屏 |
| 打包工具 | PyInstaller | 打包为 Windows 独立 .exe |

### 1.3 目标设备

- 设备型号: 华为畅享 90 Pro Max (CHZ-AL00)
- 操作系统: OpenHarmony 6.1.1.125
- 屏幕分辨率: 1128×2444 (渲染) / 1272×2756 (物理)
- 刷新率: 60/90/120Hz (当前 120Hz)
- 连接方式: USB 数据线 + USB 调试

---

## 二、系统架构

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────┐
│                    为投个屏 应用主进程                      │
│  ┌───────────────────────────────────────────────────┐  │
│  │              UI 层 (PySide6 Qt)                    │  │
│  │  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │  │
│  │  │设备页│ │投屏页│ │扩展页│ │工具箱│ │设置页│   │  │
│  │  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──────┘   │  │
│  └─────┼────────┼────────┼────────┼─────────────────┘  │
│        │        │        │        │                      │
│  ┌─────┴────────┴────────┴────────┴─────────────────┐  │
│  │              核心服务层 (Core)                      │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │  │
│  │  │HDCClient │ │HDCCastSvc│ │InputMgr  │          │  │
│  │  └──────────┘ └──────────┘ └──────────┘          │  │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐          │  │
│  │  │AudioMgr  │ │WebCastSrv│ │CastEngine│          │  │
│  │  └──────────┘ └──────────┘ └──────────┘          │  │
│  └───────────────────────────────────────────────────┘  │
│                         │                                │
│  ┌──────────────────────┴───────────────────────────┐  │
│  │              设备通信层 (HDC)                      │  │
│  │  hdc.exe ↔ USB ↔ HarmonyOS Device                │  │
│  └───────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
为投个屏/
├── main.py                  # 应用入口
├── favicon.ico              # 应用图标
├── requirements.txt         # Python 依赖
├── build.py                 # PyInstaller 打包脚本
├── build_with_nuitka.py     # Nuitka 打包脚本
├── core/                    # 核心服务层
│   ├── __init__.py
│   ├── hdc_client.py        # HDC 设备管理客户端
│   ├── hdc_cast_service.py  # HDC 投屏服务 (核心)
│   ├── cast_engine.py       # TCP 投屏引擎
│   ├── input_manager.py     # 输入管理 (键盘/剪贴板/HID)
│   ├── audio_manager.py     # 音频管理
│   └── web_cast_server.py   # 网页投屏服务
├── ui/                      # UI 界面层
│   ├── __init__.py
│   ├── styles.py            # 全局 QSS 样式
│   ├── main_window.py       # 主窗口 + 侧边栏
│   ├── device_page.py       # 设备列表页
│   ├── cast_page.py         # 投屏界面页
│   ├── extensions_page.py   # 扩展功能页
│   ├── performance_page.py  # 性能监控页
│   ├── toolbox_page.py      # 工具箱页
│   └── settings_page.py     # 设置/关于页
├── resources/
│   └── tools/
│       ├── hdc/
│       │   ├── hdc.exe      # HDC 可执行文件
│       │   └── libusb_shared.dll
│       ├── icon.ico         # 应用图标
│       └── icon.png
└── ys/xys/images/           # 设计元素图片 (Element_*.png)
```

---

## 三、核心模块详解

### 3.1 HDC 设备管理客户端 (`core/hdc_client.py`)

#### 职责
- 设备发现与列表维护
- 设备信息查询 (名称、型号、版本、分辨率)
- WiFi 无线连接
- 设备状态监控 (2秒轮询)

#### 关键类

**`HDCClient(QObject)`**
- 信号: `device_found(str)`, `device_lost(str)`, `devices_updated(list)`, `log_message(str)`
- `run_hdc(args)`: 执行 HDC 命令并返回结果
- `list_devices()`: 获取已连接设备列表
- `get_device_info(device_id)`: 查询设备详细信息
- `take_screenshot(device_id, save_path)`: 截图并保存到本地

**`DeviceMonitor(QThread)`**
- 后台线程，每 2 秒轮询设备列表
- 检测设备上线/离线，发射信号

#### HDC 命令映射

| 功能 | HDC 命令 |
|------|---------|
| 列出设备 | `hdc list targets` |
| 设备名称 | `hdc -t <id> shell param get "const.product.name"` |
| 设备型号 | `hdc -t <id> shell param get "const.product.model"` |
| 系统版本 | `hdc -t <id> shell param get "const.ohos.fullname"` |
| 屏幕分辨率 | `hdc -t <id> shell hidumper -s RenderService -a screen` |
| 截图 | `hdc -t <id> shell snapshot_display -f /data/local/tmp/x.jpeg` |
| 文件接收 | `hdc -t <id> file recv <remote> <local>` |

> **注意**: `snapshot_display` 仅支持 `.jpeg` 扩展名，不支持 `.jpg` 或 `.png`。

### 3.2 HDC 投屏服务 (`core/hdc_cast_service.py`) — 核心

#### 职责
- 实时屏幕画面捕获 (多种策略 + 自动降级)
- 触控/键盘/滑动/文本输入注入
- 设备信息查询
- 帧率统计

#### 截图策略 (按性能优先级)

1. **持久化 Shell 会话** (最优)
   - 创建一个持续的 `hdc shell` 进程
   - 设备端循环: 截图 → base64 编码 → 输出标记
   - 避免每帧创建子进程的开销
   - 使用 `FRAME_DELIM` / `FRAME_END` 标记分隔帧

2. **管道直传** (`/dev/stdout`)
   - `snapshot_display -f /dev/stdout` 直接通过 stdout 传输
   - 单次子进程调用，无文件 I/O
   - 部分设备不支持时会自动降级

3. **Base64 单次调用**
   - 截图 + base64 编码合并为一条 shell 命令
   - 比 file 方式快 2-3 倍 (省去 file recv 和 rm)

4. **文件传输** (最慢，兜底)
   - 截图到设备 → file recv 到本地 → 删除远程文件 → 本地解码

#### 输入注入

| 操作 | HDC 命令 |
|------|---------|
| 触摸按下 | `uinput -T -d <x> <y>` |
| 触摸移动 | `uinput -T -m <x1> <y1> <x2> <y2>` |
| 触摸抬起 | `uinput -T -u <x> <y>` |
| 点击 | `uinput -T -c <x> <y>` |
| 拖拽 | `uinput -T -g <x1> <y1> <x2> <y2> <press_time> <total_time>` |
| 按键按下 | `uinput -K -d <key_code>` |
| 按键抬起 | `uinput -K -u <key_code>` |
| 输入文本 | `uinput -K -t <text>` |

#### 帧率控制
- 默认 30 FPS，可配置 1-120 FPS
- 通过 `time.sleep()` 控制截图间隔
- 后台解码线程实现双缓冲流水线

### 3.3 输入管理器 (`core/input_manager.py`)

#### 键盘映射
- Qt 按键 → HarmonyOS 键码映射表
- 支持组合键: Ctrl+C/V/A/Z/X
- F1-F12 功能键映射
- 方向键映射

#### 剪贴板同步
- `ClipboardSync(QObject)` 类
- 监听系统剪贴板变化
- 通过 HDC `param set` 同步到设备

### 3.4 音频管理器 (`core/audio_manager.py`)

- `AudioManager`: 使用 sounddevice/pyaudio 采集系统音频
- `AudioStreamServer`: TCP 广播音频流到连接的客户端
- 支持: 自动静音、音量控制、音频电平监控

### 3.5 网页投屏服务 (`core/web_cast_server.py`)

- HTTP 服务 (端口 8080): 提供设备查看页面
- WebSocket 服务 (端口 8081): 实时推送视频帧
- `PermissionManager`: 令牌管理，支持权限模板
  - `view_only`: 仅查看
  - `standard`: 查看 + 触控
  - `full_control`: 完全控制

---

## 四、UI 界面层

### 4.1 主窗口 (`ui/main_window.py`)

- 侧边栏导航: 设备投屏 / 设备技能 / 扩展功能
- 页面栈: device(0) → skill(1) → extensions(2) → cast(3) → settings(4)
- 自定义组件: RippleDecor, StatusDots, NavItemWidget
- 初始化: HDCClient, HDCCastService, InputManager, AudioManager

### 4.2 设备列表页 (`ui/device_page.py`)

- 设备卡片: 名称、型号、版本、分辨率、在线状态
- 自动刷新设备列表 (启动后 120ms 延迟刷新)
- WiFi 连接功能

### 4.3 投屏界面页 (`ui/cast_page.py`)

- `PhoneScreen`: 视频帧渲染 + 鼠标/触摸映射 + 键盘事件
- `CastToolbar`: FPS 显示、旋转、音量、导航、截图、录制、分享
- 录制: 通过 OpenCV VideoWriter 保存 MP4
- 设置面板: 模式选择 (JPEG/H.264) + FPS 限制

### 4.4 扩展功能页 (`ui/extensions_page.py`)

三列卡片布局:
1. **输入法服务**: 开关、测试输入、快捷键标签
2. **音频同步**: 波形显示、音量滑块、自动静音
3. **HID 键鼠模式 (Beta)**: 开关、灵敏度控制

### 4.5 性能监控页 (`ui/performance_page.py`)

- 6 个指标卡片: FPS、CPU、GPU、内存、电量、温度
- 每个卡片含 MiniChart 迷你折线图
- 500ms 采集间隔

### 4.6 工具箱页 (`ui/toolbox_page.py`)

三个标签页:
1. **终端**: HDC 命令执行
2. **文件管理**: 本地/设备文件浏览与传输
3. **日志查看**: 实时日志流

### 4.7 设置页 (`ui/settings_page.py`)

- 左侧导航: 通用设置 / 关于软件
- 关于页: 软件介绍、功能亮点、技术栈、作者信息、致谢

---

## 五、数据流

### 5.1 投屏数据流

```
HarmonyOS 设备
    │
    ▼ (snapshot_display 截图)
hdc shell (持久化会话)
    │
    ▼ (base64 编码帧)
HDCCastService._read_persistent_frame()
    │
    ▼ (cv2.imdecode → RGB ndarray)
frame_received 信号
    │
    ▼
CastPage.PhoneScreen.update_frame()
    │
    ▼ (QPainter.drawImage)
Qt 窗口渲染
```

### 5.2 触控数据流

```
用户鼠标点击 PhoneScreen
    │
    ▼ (坐标映射: 窗口坐标 → 设备坐标)
HDCCastService.send_touch(x, y, action)
    │
    ▼ (组装 HDC 命令)
hdc -t <id> shell uinput -T -d <x> <y>
    │
    ▼
HarmonyOS 设备执行触摸事件
```

---

## 六、构建与部署

### 6.1 开发环境

```bash
# Python 3.10+
pip install PySide6 numpy opencv-python websockets Pillow sounddevice pyaudio
```

### 6.2 运行

```bash
python main.py
```

### 6.3 打包

```bash
python build.py
# 输出: dist/为投个屏.exe
```

### 6.4 打包配置

- 模式: `--onefile --windowed` (单文件无控制台)
- 资源: resources/ 目录 + ys/xys/images/ + favicon.ico
- 隐藏导入: cv2, numpy, websockets, 所有 core/* 和 ui/* 模块
- 图标: favicon.ico

---

## 七、已知问题与修复记录

### 7.1 已修复 (v2.0.0)

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1 | `take_screenshot` 使用 `.png` 扩展名 | hdc_client.py | 改为 `.jpeg` |
| 2 | `get_device_screen_size` 解析 `snapshot_display -h` 输出 | hdc_cast_service.py | 改用 `hidumper -s RenderService -a screen` |
| 3 | `get_device_info` 使用错误的版本参数名 | hdc_cast_service.py | 改为 `const.ohos.fullname` |
| 4 | `performance_page.py` QPoint 未导入 | performance_page.py | 添加 import |
| 5 | `toolbox_page.py` 暗色主题不匹配 | toolbox_page.py | 改为亮色主题 |
| 6 | `toolbox_page.py` QTextCharFormat 导入过晚 | toolbox_page.py | 移到文件顶部 |
| 7 | 性能监控使用随机模拟数据 | performance_page.py | 接入真实 HDC 数据 |
| 8 | 工具箱终端未通过 HDC 执行 | toolbox_page.py | 使用 hdc shell 执行命令 |
| 9 | 文件管理使用模拟数据 | toolbox_page.py | 接入 HDC file 命令 |

### 7.2 已修复 (2026-08-03)

| # | 问题 | 文件 | 修复 |
|---|------|------|------|
| 1 | 投屏画面始终空白：服务端已改为"单槽缓冲 + 按需拉取"，但 UI 仍在等 `frame_received` 信号（该信号不再发射） | hdc_cast_service.py / cast_page.py | CastPage 增加定时器拉取 `get_latest_frame()`，按帧版本号去重 |
| 2 | 网页投屏帧广播循环为空 (`pass`)，WebSocket 客户端收不到任何画面 | web_cast_server.py | 记录每个客户端的 websocket 对象，广播循环按 ~30FPS 推送 JPEG 帧；断线自动清理；stop() 干净关闭服务 |
| 3 | 持久化 shell 进程退出后投屏不降级，画面卡死 | hdc_cast_service.py | 检测到 shell 已退出时清理引用并降级到 base64/文件截图路径 |
| 4 | Ctrl+C/V/X/A/Z 组合键分支不可达（Ctrl 按下时 `event.text()` 为控制字符） | input_manager.py / cast_page.py | 在 `isprintable()` 检查前按 `event.key()` 处理组合键，Ctrl+V 走剪贴板粘贴 |
| 5 | 截图/录屏颜色红蓝互换（BGR 帧按 RGB 保存/转换） | cast_page.py | 截图用 `Format_BGR888`；录屏直接写入 BGR 帧，不再做颜色转换 |
| 6 | 触摸移动命令只有 2 个坐标，与 `uinput -T -m` 要求的 4 坐标不一致 | hdc_cast_service.py / scrcpy_control.py | 统一为 `-m x y x y` |
| 7 | 性能页/工具箱把 "设备ID device" 整行当作设备 ID，HDC 命令失败 | performance_page.py / toolbox_page.py | 只取行首设备 ID |
| 8 | 工具箱文件下载把 "文件名 (大小)" 整串当路径 | toolbox_page.py | 用 `Qt.UserRole` 保存真实文件名；目录跳转同样修正 |
| 9 | WiFi 连接按钮只弹提示不实际连接 | device_page.py | 调用 `connect_device()` 并刷新设备列表 |
| 10 | 配置弹窗提供 90/120 FPS，但服务端上限 60 会被静默截断 | cast_config_dialog.py | 帧率选项收敛到 15-60 FPS |
| 11 | 性能页内存解析失败时不回退 `/proc/meminfo` | performance_page.py | 解析失败自动回退 |
| 12 | 测试脚本引用不存在的 `phone_shell`、写死旧路径，GBK 控制台打印 Unicode 崩溃 | test_ui_full_layout.py | 修正属性名、路径动态化、stdout 转 UTF-8 |
| 13 | `.spec` 写死 `D:\为投个屏` 旧路径 | 为投个屏.spec | 改为相对 spec 目录 |
| 14 | Nuitka 打包排除了运行时会被引用的 web_cast_server / cast_engine | build.py | 移除这两条排除 |
| 15 | `apply_cast_config` 存在 `or True` 恒真条件 | hdc_cast_service.py | 改为明确的重启逻辑 |
| 16 | 设备记住的配置为 H.264 流模式，但该模式尚未落地，每次投屏先等 15s 超时再降级 | cast_config.py / cast_configs.json / hdc_cast_service.py | 默认捕获模式改为 JPEG；该设备配置改为 jpeg；流模式等待窗口缩短到 5s，降级不再弹错误框 |

---

## 八、HDC 命令参考

### 8.1 设备管理

```bash
hdc list targets                              # 列出设备
hdc -t <deviceId> shell param get <param>     # 获取系统参数
hdc -t <deviceId> shell hidumper -s RenderService -a screen  # 屏幕信息
```

### 8.2 截图

```bash
hdc -t <id> shell snapshot_display -f /data/local/tmp/x.jpeg  # 截图
hdc -t <id> shell snapshot_display -f /dev/stdout             # 管道输出
hdc -t <id> file recv <remote> <local>                        # 接收文件
```

### 8.3 输入注入

```bash
# 触摸
hdc -t <id> shell uinput -T -d <x> <y>          # 按下
hdc -t <id> shell uinput -T -u <x> <y>          # 抬起
hdc -t <id> shell uinput -T -c <x> <y>          # 点击
hdc -t <id> shell uinput -T -m <x1> <y1> <x2> <y2>  # 移动
hdc -t <id> shell uinput -T -g <x1> <y1> <x2> <y2> <press> <total>  # 拖拽

# 键盘
hdc -t <id> shell uinput -K -d <key>            # 按下
hdc -t <id> shell uinput -K -u <key>            # 抬起
hdc -t <id> shell uinput -K -t <text>           # 输入文本

# 鼠标
hdc -t <id> shell uinput -M -m <dx> <dy>        # 移动
hdc -t <id> shell uinput -M -c <button>         # 点击
hdc -t <id> shell uinput -M -s <scroll>         # 滚动
```

### 8.4 文件操作

```bash
hdc -t <id> shell ls <path>                     # 列出文件
hdc -t <id> file recv <remote> <local>          # 下载
hdc -t <id> file send <local> <remote>          # 上传
hdc -t <id> shell rm <path>                     # 删除
```

### 8.5 性能数据

```bash
hdc -t <id> shell hidumper -s ResourceManager -a "meminfo"  # 内存
hdc -t <id> shell "cat /sys/class/power_supply/battery/capacity"  # 电量
hdc -t <id> shell "cat /sys/class/thermal/thermal_zone*/temp"  # 温度
hdc -t <id> shell "top -n 1"                   # CPU 使用率
```

---

## 九、设计规范

### 9.1 色彩系统

| 色彩 | 值 | 用途 |
|------|-----|------|
| 主色 | #007DFF | 品牌色、按钮、选中状态 |
| 主色悬停 | #0066CC | 悬停状态 |
| 主色按下 | #0052D9 | 按下状态 |
| 主色浅 | #E6F0FF | 背景、选中浅色 |
| 主色边框 | #80BFFF | 边框高亮 |
| 文字主 | #182431 | 主要文字 |
| 文字次 | #99A2B1 | 辅助文字 |
| 文字弱 | #C4C6CC | 描述文字 |
| 背景 | #F1F3F5 | 页面背景 |
| 卡片背景 | #FFFFFF | 卡片背景 |
| 边框 | #E5E8EB | 默认边框 |
| 警告 | #FA9E3B | Beta 标签 |
| 错误 | #FA2A2D | 错误状态 |

### 9.2 字体

- 主字体: HarmonyOS Sans SC, Microsoft YaHei, PingFang SC, Segoe UI
- 等宽字体: Consolas, Monaco, monospace
- 标题: 26-28px, 700 weight
- 正文: 13-14px, 500-600 weight
- 辅助: 11-12px

### 9.3 圆角

- 卡片: 12px
- 按钮: 8px / 999px (胶囊)
- 输入框: 8px
- 图标容器: 50% (圆形)

---

## 十、后续规划

1. **H.264 视频流模式**: 通过设备端编码服务实现低延迟视频流
2. **多设备并行投屏**: 支持同时连接 100 台设备
3. **WiFi 无线投屏**: 摆脱 USB 线缆限制
4. **主题切换**: 支持亮色/暗色主题
5. **快捷键自定义**: 用户可自定义键盘映射
6. **录制优化**: 支持音频同步录制
7. **远程投屏**: 通过网页实现跨网络投屏

---

## 十一、HoKit 高性能投屏方案（2026-08-03 实测）

### 11.1 背景

华为畅享 90 Pro Max（CHZ-AL00 / OpenHarmony 6.1.1.125）上，传统
`snapshot_display` 截图方案只有 1~2 FPS，而 HoKit 投屏"流畅够用"。
通过对 HoKit 安装目录（`C:\Program Files\HoKit\resources`）逆向分析，
确认其性能来源是**设备端 uitest 扩展 + 连续截图流**，而非系统截图命令。

### 11.2 关键发现

1. **so 文件是加密的**：HoKit 的 `screencopy_*.so` / `agent_*.so` 均为
   AES-256-CBC 加密（IV=文件前 16 字节，密钥内置于 app.asar 主程序），
   推送前在电脑端解密。此前直接推送加密文件导致加载失败（MUSL-LDSO
   XPM 报错），解密后一切正常。
2. **JPEG 截图流（agent.so）**：`uitest start-daemon singleness` 自动加载
   `/data/local/tmp/agent.so`，通过 UITest RPC（`uitest_socket` 抽象套接字）
   发送 `startCaptureScreen`，agent 用 Rosen `GetScreenshotWithOption` +
   libjpeg 连续推帧。实测 scale=0.5 → **约 7 FPS**（scale=0.99 只有 0.4 FPS）。
3. **H.264 视频流（screencopy_server.so）**：`uitest start-daemon singleness
   --extension-name scrcpy_server.so -scale X -frameRate F -bitRate B -p 9958
   -screenId 0 -encodeType 0 -iFrameInterval I -repeatInterval 33`，
   通过 gRPC（`scrcpy_grpc_socket`）拉 Annex-B H.264 流。HoKit 在
   本设备实测配置：scale=2(50%)、frameRate=30、bitrate=10MB/s、分辨率
   564x1222，画面流畅。
4. HoKit 在消费级 HarmonyOS 设备上 `H264=true`，H.264 模式可用。

### 11.3 本项目落地

新增资源与模块：

- `resources/tools/so/arm64-v8a/`：`agent_v2.so`、`agent_v1.so`、
  `screencopy_v2.so`、`screencopy_v1.so`（HoKit 原厂 so 解密后的明文 ELF）
- `resources/tools/so/x86_64/`：`agent.so`、`screencopy.so`
- `core/hokit_jpeg_channel.py`：agent JPEG 截图流通道
- `core/hokit_h264_channel.py`：scrcpy_server gRPC H.264 流通道
  （含 onEnd 复位、onStart 拉流、静止 15s 软刺激、25s 唤醒）
- `core/hokit_grpc/`：ScrcpyService gRPC 桩（scrcpy.proto 生成）

接入方式：

- 配置 `capture_mode` 新增 `agent_jpeg`（配置对话框"⚡ 高性能"卡片），
  `cast_engine_mode` 映射到 `agent_jpeg`；
- `h264` 模式（`stream`）已改为 HoKit 同款 gRPC 链路，
  H.264 数据直接送现有 H264Decoder(PyAV) 解码渲染；
- 依赖新增 `grpcio>=1.60.0`。

### 11.4 实测数据（华为畅享 90 Pro Max）

| 方案 | 缩放 | 帧率 |
|------|------|------|
| snapshot_display（旧 JPEG） | 100% | 1~2 FPS |
| agent JPEG 流（新） | 50% | 画面变化时 ~7 FPS |
| H.264 gRPC 流（新） | 50% | 30 FPS 配置（画面变化时流畅） |

H.264 通道完整链路已验证：推送 so → 启动 daemon → 等待 RPC 套接字 →
端口转发 → gRPC onEnd/onStart → 持续收到 Annex-B H.264 包并解码出
564x1222 帧 → 停止时自动清理 daemon 与转发规则。

### 11.6 过程修复的既有问题

1. **H264Decoder 初始化 bug**：`av.CodecContext.create(..., mode="decode")`
   应为 `mode="r"`（PyAV 只接受 "r"/"w"），导致 H.264 解码器从未成功创建。
2. **fport 删除语法**：`hdc fport rm` 需同时带本地端口与远端目标两端，
   只传一端会报 "ruler is not exist" 且规则残留。
3. **pgrep 自匹配**：`pgrep -f scrcpy_server.so` 会匹配到自身 shell，
   改用 `ps -ef | grep ... | grep -v grep` 判断 daemon 状态。
4. **daemon 启动时序**：进程出现 ≠ RPC 套接字就绪，过早建转发会拿到空流，
   增加"等待抽象套接字 + 无数据自动 IDR/重启流"自愈逻辑。

### 11.5 注意事项

- 设备端 so 来自 HoKit，仅供本地学习/自用；商用或分发前建议自研
  同协议 uitest 扩展（OpenHarmony NDK）替换。
- uitest daemon 同一时刻只服务一个投屏客户端；本项目与 HoKit
  同时使用同一台设备会互相抢占 daemon。
- 设备系统版本升级后若 uitest 协议变化，需同步更新 so 与协议桩。

---

## 十二、性能实测与优化（2026-08-03 二轮）

### 12.1 背景

首轮已实现 HoKit 同款 H.264 gRPC 链路，但未做性能基准与管线优化。
本轮以「性能超过 HoKit」为目标，用华为畅享 90 Pro Max 真机实测并优化。

### 12.2 关键发现（实测）

1. **设备端编码器按需产帧**：画面静止时编码器不产帧（0 FPS），
   画面变化时按 `-frameRate` 请求速率满速产帧。因此「平均帧率」低
   不代表性能差，应以「画面变化时有效帧率 + 端到端延迟」衡量。
2. **repeatInterval 决定编码节奏**：scrcpy_server 的
   `-repeatInterval 33`（HoKit 默认）在动画期间设备端约 51 FPS；
   改为 `16ms` 后提升到 **77 FPS**（峰值 98）；`8ms` 时达到
   **115 FPS**（峰值 162）。设备端（120Hz 屏）编码能力远超 HoKit 配置。
3. **gRPC 帧实时到达**：消息带 `pts`（设备端时间戳），动画期间
   每帧间隔约 33ms（repeat=33）或更密（repeat=16/8）。帧是实时推送的，
   不存在「批量缓冲」——此前测到的「150~240 帧突刺」是消费线程被
   uinput 滑动命令阻塞导致的队列积压假象。
4. **端到端延迟极低**：消费线程独立时，滑动抬手（up）→ 解出首帧
   仅 **8~18ms**；启动耗时约 2.7s（含 so 推送/daemon/gRPC）。
5. **PyAV 软件解码** 564x1222@60 每帧约 5ms，单线程可承受 60 FPS
   渲染上限；更高帧率需跳帧。

### 12.3 优化项（本次代码改动）

| # | 改动 | 文件 |
|---|------|------|
| 1 | `repeat_interval` 构造参数（默认 16ms，可配置 8~100） | core/hokit_h264_channel.py |
| 2 | gRPC 队列「丢旧保新」：队满时弹出最旧帧保留最新帧（低延迟优先） | core/hokit_h264_channel.py |
| 3 | `drain_to_latest()`：burst 跳帧——积压 >2 包时排空到最新帧，只解最新帧（花屏由解码器错误隐藏 + 下一 IDR 恢复） | core/hokit_h264_channel.py |
| 4 | so 智能推送：设备端 MD5 比对直接启动（免推送秒开）、成功版本缓存、多版本回退 | core/hokit_h264_channel.py |
| 5 | `set_repeat_interval()` + 配置链路（CastConfig.repeat_interval） | core/hdc_cast_service.py、core/cast_config.py |
| 6 | 配置弹窗新增「编码节奏」档位（标准 33 / 高性能 16 / 极速 8），仅 H.264 模式可用 | ui/cast_config_dialog.py |
| 7 | `_h264_grpc_loop` 接入 burst 跳帧（积压时排空到最新） | core/hdc_cast_service.py |

### 12.4 实测数据（华为畅享 90 Pro Max / OpenHarmony 6.1.1.125，scale=2 50%）

| 配置 | 设备端产帧（动画期间） | 说明 |
|------|----------------------|------|
| HoKit 默认 repeat=33 / 60fps | ~51 FPS（峰值 65） | 与 HoKit 实际体验一致 |
| **本项目默认 repeat=16 / 60fps** | **~77 FPS（峰值 98）** | 约为 HoKit 的 2.5 倍 |
| 极速 repeat=8 / 60fps | ~115 FPS（峰值 162） | 设备压力较大，发热 |
| 帧率配置 120fps | 88.8 FPS | 受 repeat 16 限制 |

端到端延迟：滑动抬手 → 首帧渲染 **8~18ms**；通道启动 ~2.7s（第二次起
设备 so MD5 匹配免推送，秒开）。

### 12.5 验证工具（保留在项目根目录）

- `bench_h264.py`：H.264 通道性能基准（STIM=move/gesture/burst/none）
- `probe_latency.py`：端到端延迟测试（真实场景：滑动在子线程，消费循环独立）
- `probe_pts.py`：设备端产帧率测量（对比 repeat_interval）
- `test_h264_e2e.py`：配置→通道→解码→渲染全链路集成测试
- `test_config_dialog.py`：配置弹窗冒烟测试（编码节奏档位保存/持久化/旧配置兼容）

### 12.6 已知限制

- 设备端音频同步（AudioCaptureAbility + rport 9960）依赖手机端
  `com.hokit.app`，该设备未安装，无法复刻；项目保留 PC 端音频方案。
- `uinput -T -g`（拖拽手势）在当前系统版本报 "total time input is error"，
  改用 down/move/up 序列实现滑动。
- 极速档（repeat=8）会显著提高设备端编码负载，默认不开启。

### 12.7 二轮 review 修复（2026-08-03）

`review` / `security-review` 审查后修复的问题：

| # | 问题 | 修复 |
|---|------|------|
| 1 | `CastConfig._load_all` 漏 `repeat_interval` 字段：记住"极速(8)"档后重启程序静默回落到默认 16 | 补 `repeat_interval=int(obj.get(...))`，新增磁盘恢复测试（`test_disk_restore_preserves_repeat`） |
| 2 | `_restart_stream` 哨兵竞态：排空队列发生在 `feeder.join()` 之前，旧 feeder 的 `finally` 会 `put(None)`，导致自愈重启后 `read_packet` 首包取到 None、H.264 流静默死亡 | 排空挪到 join 之后，哨兵不残留 |
| 3 | UI 档位名"极速 (120FPS 级)"与 `frame_rate` 上限 60 矛盾，有误导 | 改为按真实参数命名：`标准 (33ms)` / `高性能 (16ms)` / `极速 (8ms)` |
| 4 | `send_text` 回退路径将用户文本未转义作为 argv 传入（文本含空格/引号/分号即成为设备端任意命令） | 回退路径与主路径一致：单引号包裹 + `'\''` 转义 + 过滤 `\r\n` |
| 5 | `send_clipboard` 剪贴板内容（不可信）未转义拼进 `hdc shell param set` | 同款转义处理 |

真机验证：`uinput -K -t 'a'\''b; echo HACKED'` 无 HACKED 输出（分号被单引号包裹为字面量，注入被阻止）。

### 12.8 触控链路修复（2026-08-03，用户反馈"投屏后鼠标不能操作滑动"）

排查与修复：

| # | 问题 | 修复 |
|---|------|------|
| 1 | **hdc shell 持久化进程无效**：`hdc shell`（无参数）在该设备报 `Not support stdio TTY mode`，stdin 写入的命令从不执行 → 所有触控事件静默丢失 | `_send_input_cmd` 改为单次 `hdc shell <cmd>` 子进程执行；`_start_input_shell` 改为占位（不再创建挂死进程） |
| 2 | **action 语义错位**：GUI 传 `down=0/move=1/up=2`，而 scrcpy 常量是 `DOWN=0/UP=1/MOVE=2` → move 被映射成 up、up 被映射成 move | `cast_page._on_touch_event` action_map 改为 `{down:0, up:1, move:2}`；`send_touch` 按 scrcpy 语义分派 |
| 3 | **move 命令原地不动**：`uinput -T -m x y x y`（起点=终点）→ 滑动无效 | move 维护触摸状态，生成 `-m 起点X 起点Y 终点X 终点Y`（`HDCCastService._touch_x/_touch_y` + `ControlQueue._touch_state`） |
| 4 | **子进程开销**：单次 `hdc shell` 约 150ms，滑动高频事件会积压 | `ControlQueue` 批量发送：60ms 收集窗口攒事件、批内连续 move 只留最新、`; ` 连接多条命令一次发送 |

真机验证（华为畅享 90 Pro Max）：滑动前→后像素变化占比 **0.01% → 31.75%**（设置页滑动明显生效）。

新增回归测试：`TestTouchControl`（action 语义 + move 起点命令生成），防止语义再错位。

### 12.9 独立投屏窗口 + 延迟优化（2026-08-03）

#### 独立投屏窗口（复刻 HoKit ScreencastWindow）

新增 `ui/cast_window.py` — `CastWindow`：无边框独立顶层窗口，
设备页点"投屏"即打开（`main_window._goto_cast` 改为启动独立窗口，
不再切主窗口内嵌页；`cast_page` 保留兜底）。

布局：
- 顶部自绘标题栏：设备名 + 实时 FPS + 最小化/关闭（可拖拽移动窗口）
- 中央 `PhoneScreen` 画布（自由缩放窗口）
- 右侧 `CastToolbar` 侧边工具栏：帧率、返回、桌面、最近任务、电源、
  旋转、截图、录制、音量±、静音、网页分享（全部接线可用）

#### 延迟优化（实测验证）

| # | 问题 | 修复 |
|---|------|------|
| 1 | **渲染帧率上限 30FPS**：内嵌页 `_frame_timer` 33ms | 独立窗口与内嵌页统一 16ms（60FPS） |
| 2 | **VideoRegulator 每帧 +50ms 缓冲**：本项目 gRPC PTS 恒为 0，`delay=0.05` 的 PTS 时钟缓冲纯增延迟 | `_h264_grpc_loop` 与 TCP 引擎解码后直接 `_push_frame`（移除 regulator） |
| 3 | **点击响应 +60ms**：ControlQueue 无条件 60ms 收集窗口 | 仅队列含 move（滑动场景）才等收集窗口；点击/按键立即发送 |
| 4 | **静止黑屏**：H.264 编码器静止不产帧，投屏打开后无画面 | 启动保底：3s 无帧则 `_take_screenshot()` 补一张即时截图作为首帧 |
| 5 | **默认模式帧率低**：设备配置为 agent_jpeg（~7FPS） | 设备默认配置切换为 h264 + 高性能（repeat=16）—— 动画期间实测 31~77FPS |

#### 实测（华为畅享 90 Pro Max，H.264 / scale=2 / repeat=16）

- 独立窗口启动投屏 ~1.1s（通道已 MD5 免推送秒开）
- 静止桌面：立即显示首帧（截图保底），不黑屏
- 设置页滚动动画：标题栏帧率 31 FPS（帧版本 22/6s），操作跟手
- 滑动/点击触控链路：滑动画面变化 38.15%（此前触控修复已验证）

新增验证工具：`test_cast_window.py`（独立窗口冒烟：布局/接线/帧渲染）、
`test_cast_window_e2e.py`（真机端到端：启动→帧→滚动帧率→触控→关闭）。

### 12.10 独立窗口 review 修复（2026-08-03）

`review` 审查独立窗口/延迟改动后修复：

| # | 问题 | 修复 |
|---|------|------|
| 1 | **start_casting 非幂等**：共享 HDCCastService 实例下重复触发（双击/内嵌页冲突）会把运行中的投屏停掉 | 服务已在投屏且同设备 → `show()+raise_()` 复用；被其它入口占用 → 提示先停止 |
| 2 | 首帧保底置位过早（有包即置位，解码失败时保底失效） | `got_first_frame` 移到 `_push_frame` 成功后 |
| 3 | 录屏内存峰值：1800 帧 × 全分辨率 ≈ 14GB | 流式写临时文件（`cv2.VideoWriter`），停止时 move 到用户路径 |
| 4 | stop 顺序：先 join 线程再 `channel.stop()`，超时后旧线程可能污染新会话 | 先 `h264_channel.stop()`（cancel 流）再 join 线程 |
| 5 | closeEvent 录制中关闭残留临时 mp4 | 关闭时 release writer + 删临时文件 |

review 最终 verdict：**pass**（遗留 nit 不阻断：录屏固定 30fps，agent_jpeg ~7fps 回放会加速，H.264 主模式不受影响）。

### 12.11 安全审查修复（2026-08-03）

`security-review` 发现 3 个 MEDIUM（无 HIGH/CRITICAL），已修复：

| # | 问题 | 修复 |
|---|------|------|
| 1 | 录制临时文件 `weitouping_rec_{时间戳}.mp4` 可预测，存在符号链接/覆写攻击面；同秒两次录制互相覆写 | `tempfile.mkstemp(prefix="weitouping_rec_", suffix=".mp4")` 独占创建唯一文件 |
| 2 | 截图临时文件 `temp_sc_{0..3}.jpeg` / `temp_screenshot.jpeg` 固定名复用写 BASE_DIR，recv 失败分支不清理 | `_take_screenshot_fast` / `_take_screenshot` 改 `mkstemp` 唯一名 + `try/finally` 全路径清理 |
| 3 | 网页分享绑定 `0.0.0.0`（HTTP/WS 全接口），局域网任意主机凭 token 可 24h 完全控制设备；UI 展示 localhost 造成误解 | 默认与显式均绑 `127.0.0.1`（仅本机可访问），注释说明跨网络需显式改 `0.0.0.0` 并承担风险 |

security-review 最终 verdict：**pass**（无未解决安全问题）。

### 12.12 用户反馈"投屏卡顿/无法滑动"修复（2026-08-05）

用户复测反馈：投屏后画面卡顿、鼠标滑动困难。排查与修复：

| # | 根因 | 修复 |
|---|------|------|
| 1 | **设备配置被写回 agent_jpeg 且该通道启动失败**（`等待 uitest_socket 超时`）：设备残留 h264 scrcpy daemon，`uitest start-daemon singleness` 全局单实例复用旧 daemon → agent 的 uitest_socket 不出现 | ①daemon 归属检测：agent 通道增加 `_agent_daemon_running`（命令行含 `scrcpy_server.so` 视为他人）/`_kill_other_daemons`；h264 通道对称处理 —— 模式切换不再失败；②配置文件恢复 h264 |
| 2 | **h264 daemon 被误杀**：h264 的 `_kill_other_daemons` 原按 `scrcpy_server.so` 保留、其余全杀 —— agent daemon（无 `--extension-name`，命令行不含 agent.so）判定修正 | 两个通道的 daemon 归属判定统一：h264 按含 `scrcpy_server.so` 判定，agent 按不含 `scrcpy_server.so` 判定（默认 extension） |
| 3 | **滑动命令延迟**：hdc shell 单命令实测 ~103ms，批量窗口 60ms 时一次滑动分多批发送（跟手度不足） | 收集窗口 60→90ms：一次滑动 4~6 个 move 合并为 1~2 批发送，跟手度提升 |
| 4 | 持久 shell 方案（stdin 交互）验证不可行 | hdc 3.2.0c 不支持 TTY stdin，维持"单命令 + 批量合成"方案 |

#### 真机端到端验证（华为畅享 90 Pro Max）

- **GUI 触控注入**（`test_gui_touch.py`）：QTest 模拟鼠标按下→拖动→释放，命令序列完整
  `down(560,849) → move×4(起点→终点) → up(560,364)`，坐标映射正确（画布→设备全分辨率）
- **端到端滑动**（`test_e2e_touch.py`）：GUI 拖动 → 设备端设置页画面实际滑动，
  **变化占比 27.87%**；发送耗时 0.22s
- agent 通道（修复后）：start 4.07s 成功（此前超时失败）；h264 通道：正常启动/投屏
- 全量回归通过（unittest 19 项、核心功能 24 项、H.264 端到端、触控链路、独立窗口、配置弹窗）

### 12.13 输入通道 review 修复（2026-08-05）

`review` 审查输入通道后修复（最终 verdict：pass）：

| # | 问题 | 修复 |
|---|------|------|
| 1 | 收集窗口 90ms 无条件等待，同批 down/up 被拖累（点击延迟 60→90ms） | 仅"纯滑动批"（含 move 且无 down/up/key）才等窗口；点击/按键立即发送 |
| 2 | ps -ef 解析取 `parts[1]` 为 PID，首列 USER（"shell"）列错位时可能误判/误杀 | PID 取"第一个 `isdigit()` 且 >1 的字段"，4 处 kill 逻辑统一 |
| 3 | agent daemon 复用旧内存（推送新 agent.so 后不生效） | socket 等待失败自愈：杀 daemon → 重启 → 再等 |
| 4 | `_kill_other_daemons` 固定 sleep(0.3) 拖慢启动 | 仅实际有清理动作时等待 |

新增单测 `TestDaemonOwnership`（mock ps 输出验证归属判定与 PID 解析：只杀目标 daemon、跳过 USER/错位列/PPID）。

### 12.14 输入通道安全审查修复（2026-08-05）

`security-review` 审查输入通道后修复（最终 verdict：pass）：

| # | 级别 | 问题 | 修复 |
|---|------|------|------|
| 1 | MEDIUM | ps -ef 解析"第一个纯数字字段"在 USER 列为数字 UID（OpenHarmony app uid 10000 常见）的设备上会把 UID 当 PID 误杀 | 取固定列 `parts[1]`（POSIX 第 2 列恒为 PID）+ 回查 `/proc/<pid>/cmdline` 复核含 `uitest` 才 kill；4 处 kill 逻辑统一，双层过滤只收窄不扩大 |
| 2 | LOW | h264 `screen_id` 构造器未 clamp 就拼入设备端 shell | `max(0, int(screen_id))`，与 scale/frame_rate 一致防御 |

安全审查结论：kill 命令注入面不存在（PID 经 `isdigit()` 过滤 + `/proc` 复核，`cat`/`kill` 拼串无注入）；单测 `TestDaemonOwnership` 同步覆盖 `/proc` 复核路径。

### 12.15 残影根因与 H.264 解码链路修复（2026-08-05）

**A. 全屏残影真根因（用户实操作监测定位）**
- 现象：实监测窗口四角恒白 255、操作时画面滞后（首变延迟 8.5s）
- 根因：`paintEvent` 里 `QImage(self._frame.data, ...)` **仅引用不拷贝**；
  推帧线程单变量覆盖 `_latest_frame` + 旧数组 GC 释放 → QImage/缓存 QPixmap
  悬挂（dangling pointer）→ 绘制读到被复用的内存 → 全屏残影/花屏
- 修复：`set_frame` 深拷贝隔离推帧线程；QImage 依赖 `self._frame` 生命周期
  （实例属性，本帧内必有效），`QPixmap.fromImage` 深拷贝供缓存独立持有；
  省一次 QImage 拷贝/帧（每帧共 2 次：set_frame copy + fromImage 深拷贝）
- 回归：`test_ghosting.py` 场景 4/4b（buffer 复用 + 覆盖隔离）通过

**B. H.264 0 帧（解码链路 flags 丢失）**
- 现象：e2e/实机 0 帧，但 gRPC 流有完整 NAL（SPS/IDR/P 帧）
- 根因：`_feed` 只取 payload["data"]，**丢弃 payload["flags"]**（scrcpy
  demuxer 格式，bit62=CONFIG）→ PacketMerger 无法缓存 config 包 →
  解码缺 SPS/PPS → 0 帧
- 修复：`_feed` 提取 flags 与 data 一并入队；`read_packet`/`drain_to_latest`
  返回 `(data, pts_flags)` 元组；消费端解包后 `decode_packet(data, flags)`
- 验证：e2e 0 帧 → 407~425 帧（27~28 FPS）；所有消费者（agent_jpeg/screenshot
  旧引擎）经 review 确认不消费元组，无破坏

**C. 监测工具**：`monitor_cast.py`（窗口查找/帧变化率/卡顿/四角残影指标）、
`test_slide_flow.py`（设备端滑动+窗口同步量化）；设备端滑动实测
before→mid 像素差 89.6（链路有效）
